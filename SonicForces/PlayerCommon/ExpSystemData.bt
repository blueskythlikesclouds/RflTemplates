//----------------------------------------//
// This file was auto-generated by Skyth. //
//----------------------------------------//

typedef struct {
	local uint64 p<hidden=true> = FTell();

	FSeek(p + 0); uint nextLevelNecessaryPoint[29]<optimize=false>;

	FSeek(p + 116);
} ExpLevelData;

typedef struct {
	local uint64 p<hidden=true> = FTell();

	FSeek(p + 0);   ExpLevelData level;
	FSeek(p + 116); float stageCoefficient;

	FSeek(p + 120);
} ExpSystemData;

FSeek(0x40);
ExpSystemData data;