typedef struct {
    long pointer;
} cstring<read=ReadCString>;

string ReadCString(cstring& str) {
    if (str.pointer > 0) return ReadString(64 + str.pointer);
    return "(null)";
}

typedef struct
{
    local uint64 p<hidden=true> = FTell();

    FSeek(p + 0);  cstring name;
    FSeek(p + 16); BakeCameraParameter data;

    FSeek(p + 48);
} BakeCameraParameter;

LittleEndian();

FSeek(64);
BakeCameraParameter data;
