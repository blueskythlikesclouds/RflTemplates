typedef struct
{
    local uint64 p<hidden=true> = FTell();

    FSeek(p + 0); float baseHeight;

    FSeek(p + 4);
} ObjWarshipDashRingConfig;

LittleEndian();

FSeek(64);
ObjWarshipDashRingConfig data;
